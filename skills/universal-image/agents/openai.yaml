interface:
  display_name: "通用图片生成"
  short_description: "让Codex通过可配置图片供应商MCP生成并显示图片"
  default_prompt: "使用$universal-image生成一张图片，并把生成文件显示在当前对话中。"

dependencies:
  tools:
    - type: "mcp"
      value: "universal-image"
      description: "本地通用图片生成stdio MCP服务器"
      transport: "stdio"
