interface:
  display_name: "Wawapi图片生成"
  short_description: "让不支持图片的Codex API通过MCP生成并显示图片"
  default_prompt: "使用$wawapi-image生成一张图片，并把生成文件显示在当前对话中。"

dependencies:
  tools:
    - type: "mcp"
      value: "wawapi-image"
      description: "本地Wawapi图片生成stdio MCP服务器"
      transport: "stdio"
