# Wawapi渠道能力与边界

[English](CAPABILITIES.en.md)｜简体中文

以下结论来自2026-08-01至2026-08-04的真实请求，是时间点证据，不是永久能力承诺。每次生成都必须以MCP返回的实际文件格式、像素和数量为准。不要仅凭一次瞬时错误扩展或收缩模型能力边界。

## 自动选型

- 文生图`auto`只会在本次模型目录实际返回的ID中选择，优先顺序为`gpt-image-2-high`、`gpt-image-2-medium`、`gpt-image-2-low`、`gpt-image-2`；当前目录只有基础模型时必须选择基础模型，不能强行使用历史变体。
- 参考图`auto`同样只在当前目录中选择，并优先`gpt-image-2`；基础模型有参考图成功证据，变体模型的参考图路由存在波动。
- 上游目录可能在1个和4个模型之间变化，也可能暂时无法验证。每个任务都应重新读取Doctor或模型列表；离线快照只说明历史，不冻结当前目录。
- 用户显式指定模型时保持该模型，不在失败后静默更换。
- MCP生成入口固定使用同步请求，避免异步提交端点增加故障面。

## 文生图证据

|模型|1024×1024|2048×2048|3840×2160|建议|
|---|---|---|---|---|
|`gpt-image-2-high`|精确PNG、精确WebP；`n=2`精确返回两张JPEG；2026-08-04一次HTTP524|精确PNG|精确PNG|文生图首选|
|`gpt-image-2-medium`|精确PNG|精确PNG|精确PNG|高分辨率备选|
|`gpt-image-2-low`|精确PNG|精确PNG|精确PNG|高分辨率备选|
|`gpt-image-2`|2026-08-04先后出现HTTP503与精确1024×1024 PNG；最新真实请求精确成功|历史实际1254×1254；另有瞬时HTTP502和真实2K HTTP503|2026-08-04两次最新真实请求均降为2048×1152并精确保持16:9；历史也曾1672×941和精确4K|当前目录只有基础模型时由`auto`选择；参考图优先|

普通HTTP502/503/524表示上游瞬时状态，不能据此写成模型“不支持”。不同模型路由在同一时间可能呈现不同状态。

## 参考图证据

|模型|实测结果|解释|
|---|---|---|
|`gpt-image-2`|历史请求曾返回1254×1254 PNG；2026-08-04真实edit_image精确返回1024×1024 PNG；另有HTTP502|2026-08-04两次独立真实参考图2K WebP请求均精确返回2048×2048，但格式回落PNG；当前路由仍可能波动|
|`gpt-image-2-high`|曾返回HTTP502；另有HTTP400误报提示词缺失|变体Multipart路由存在兼容性异常，优先基础模型|
|`gpt-image-2-low`|两次HTTP502|支持性未定，不等于永久不支持|
|`gpt-image-2-medium`|两次HTTP502|支持性未定，不等于永久不支持|

## 基础模型的比例降级

“同比例降级”不等于“精确分辨率”。下列历史结果均未达到请求像素：

|请求|实际|宽高比误差|分类|
|---|---|---:|---|
|2048×2048|1254×1254|0.0000%|同比例降级|
|3840×2160|1672×941|0.0531%|同比例降级|
|2048×1080|1727×911|0.0304%|同比例降级|
|2048×1152|1672×941|0.0531%|同比例降级|
|2560×1440|1672×941|0.0531%|同比例降级|
|4096×2160|3840×2016|0.4464%|近似同比例降级|
|4096×2304|1672×941|0.0531%|同比例降级|
|4096×4096|1254×1254|0.0000%|同比例降级|

宽高比误差不超过1%的结果会标记为比例保持或取整保持，但像素不一致仍为`degraded`。

## 格式、数量和文件边界

- 已有`gpt-image-2-high`精确返回1024×1024 WebP和两张1024×1024 JPEG的证据。
- 基础模型历史上忽略过WebP/JPEG并返回PNG；MCP按真实文件头保存，并报告格式偏差。
- 2026-08-04当前目录只有`gpt-image-2`时，真实单张`1024×1024 WebP`请求返回精确像素的PNG，状态为`degraded`；不能把它描述成WebP成功。
- 2026-08-04两次真实`3840×2160` PNG请求均返回`2048×1152` PNG，状态为`degraded`但宽高比精确保持；不能把比例保持描述成4K像素成功。
- 2026-08-04两次独立真实参考图`2048×2048 WebP`请求均返回精确`2048×2048 PNG`，状态为`degraded`；参考图尺寸成功不代表请求格式成功。请求输出名为`.webp`时，MCP会按真实格式改为`.png`。
- 基础模型历史上把`n=2`降级成一张拼图；MCP始终比较请求数量与实际独立文件数量。
- 2026-08-04当前目录只有`gpt-image-2`时，同一`count=2、1024×1024、JPEG`请求在较早批次返回1张`1254×1254 PNG`，较晚批次返回2张独立`1024×1024 PNG`。两次均为`degraded`且都没有得到JPEG，说明数量和像素会波动，不能把任一批次写成固定上限。
- `explain_image_capability`按请求数量严格筛选证据。多图预检必须显式传入`count`，并检查`expectedCountStatus`和证据中的`actualCount`；不能用单图证据推断多图能力。
- 在线解释的`selection_scope=current_catalog`表示当前目录感知选择；`offline=true`返回`selection_scope=bundled_baseline`，只能称为历史证据。若两者模型不同，必须先报告当前选择，不能把历史模型写成当前`auto`结果。
- 参考图只接受PNG、JPEG或WebP，最大10 MB。
- URL型结果逐跳拒绝本机、私网和链路本地地址，并限制重定向、下载时间和50 MB大小。
- 输出目标已存在时使用不冲突的新文件名，不覆盖用户文件。

## Doctor、重试与渠道状态

- Doctor的网络请求只探测`GET /models`模型目录，不发送可能计费的生图请求。`ready=true`表示本地运行时和Key没有明确阻断，`generation_channel_status=not_probed`表示真实生图渠道尚未检测。
- 模型目录的DNS、代理、TLS、超时、HTTP502/503或“无可用渠道”响应统一形成`model_catalog_unverified`非阻断警告。它们不能证明真实生图渠道不可用；用户已经授权生图时，Skill应继续一次真实生成。
- `network_error`保留`phase`、`target`、`network_category`和安全的`cause_code`。当前分类包括DNS解析、连接拒绝、连接重置、连接超时、TLS/证书、代理、网络不可达、套接字异常和未知网络错误。
- 只有`generate_image`或`edit_image`的真实生成阶段返回`upstream_channel_unavailable`，才能判定Key对应的图片渠道当前不可用。
- 仅在上游明确拒绝接单时安全重试一次，例如HTTP429或“没有可用图片渠道/兼容账号”。
- 真实生成阶段的`upstream_channel_unavailable`表示Key可能有效，但其上游图片渠道当前不可用。等待恢复或联系Key提供方，不要修改提示词、尺寸或模型来规避。
- 2026-08-04已对当前目录的1024×1024和2048×2048真实文生图各观察到一次HTTP503`upstream_channel_unavailable`；随后同一目录的多图请求返回了`ok=true`但降级结果，说明目录状态和生成结果都必须逐次记录。
- 普通HTTP502/503、HTTP524、网络中断或本地超时不自动重放，因为服务端是否已开始计费无法确定。
- 2026-08-04真实`2048×1080`宽幅请求返回HTTP524`upstream_timeout`且无文件；该次只记录为瞬时超时，不能改写为宽幅比例永久不支持。
- Codex默认MCP工具超时为60秒；安装器把`wawapi-image`的`tool_timeout_sec`设为600并反读验证。宿主返回`MCP error -32001: Request timed out`时，不能将其解释为Doctor结论、MCP`upstream_timeout`或真实渠道不可用，也不能在计费状态不明时自动重放。
